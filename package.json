{
  "name": "flywheel-memory-monorepo",
  "version": "2.0.5",
  "private": true,
  "description": "Local-first memory for AI agents. Select from 42 tools. No cloud. Everything connects.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "dev": "npm run dev -w @velvetmonkey/flywheel-memory",
    "bench": "npm run bench -w @velvetmonkey/flywheel-bench"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/velvetmonkey/flywheel-memory.git"
  },
  "author": "velvetmonkey",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
