Use flywheel search before reading files. Search returns frontmatter, backlinks, outlinks, headings, and snippets — often enough to answer without file reads.

This vault has saved policies in `.claude/policies/`. Use `policy action=list` to see them, `policy action=preview` to dry-run, and `policy action=execute` to run. Policies automate multi-step vault operations atomically.
