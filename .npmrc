# Harden against transient network failures in CI
# (npm defaults: fetch-retries=2, fetch-retry-maxtimeout=60000)
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=300000
